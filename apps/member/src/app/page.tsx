export default function Page() {
  return (
    <main style={{padding: 24}}>
      <h1>Lucky Spin — Member</h1>
      <p>UI wheel akan dibuat di Langkah 4. Sekarang endpoint <code>/api/claim</code> sudah aktif.</p>
    </main>
  );
}
